import { Routes } from '@angular/router';
import { ComputingPageComponent } from './components/computing-page/computing-page.component';

export const computingRoutes: Routes = [
    {
        path: '',
        component: ComputingPageComponent
    },
];